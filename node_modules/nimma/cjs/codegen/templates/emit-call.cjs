'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var builders = require('../ast/builders.cjs');
var scope = require('./scope.cjs');

function generateEmitCall(id, { parents, keyed }) {
  return builders.expressionStatement(
    builders.callExpression(scope.default.emit, [
      id,
      builders.numericLiteral(parents),
      builders.booleanLiteral(keyed),
    ]),
  );
}

exports.default = generateEmitCall;
